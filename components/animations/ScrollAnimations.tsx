"use client";

/**
 * ScrollAnimations — temporarily disabled.
 * All GSAP / ScrollTrigger references removed to improve performance.
 * Re-enable by reimplementing with Intersection Observer or Web Animations API.
 */
export default function ScrollAnimations() {
  return null;
}