import type { UseFooterGSAPProps } from './types';

/**
 * Footer animation — GSAP removed.
 * No-op stub: call sites don't need to change.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function useFooterGSAP(_props: UseFooterGSAPProps) {
  // No-op: animations disabled pending GSAP removal
}
