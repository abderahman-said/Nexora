import type { UseAboutGSAPProps } from './types';

/**
 * About section animation — GSAP removed.
 * Kept as a no-op hook so call sites don't need to change.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function useAboutGSAP(_props: UseAboutGSAPProps) {
  // No-op: animations disabled pending GSAP removal
}
